export type { SortConfig, AcademicPeriod, AcademicPeriodFormData } from 'src/lib/types';

export interface FilterState {
  search: string;
  status: string;
  semester: string;
  tahun_ajaran: string;
}
